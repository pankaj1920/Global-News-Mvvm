<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.payments.b2b">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.B2B"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".app_base.view_base.NoInternetActivity"
            android:exported="false" />
        <activity
            android:name=".global_news.views.activity.home.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".global_news.views.activity.authenticate.AuthenticationActivity"
            android:exported="false" />
        <activity
            android:name=".global_news.views.activity.splash.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>